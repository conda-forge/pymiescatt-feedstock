{% set pypiname = "PyMieScatt" %}
{% set version = "1.7.5" %}

package:
  name: {{ pypiname|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ pypiname[0] }}/{{ pypiname }}/{{ pypiname }}-{{ version }}.tar.gz
  sha256: de5d356a04e936a6b2f24897ba0fa29fe2f8cf2d6d18cac2957fd1ed50096e7c

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv

requirements:
  host:
    - python >=3.5
    - pip
  run:
    - python >=3.5
    - numpy
    - scipy
    - matplotlib
    - shapely

test:
  imports:
    - PyMieScatt

about:
  home: https://github.com/bsumlin/PyMieScatt
  license: MIT
  license_file: LICENSE.md
  summary: A collection of forward and inverse Mie solving routines based on Bohren and Huffman's Mie Theory derivations.

extra:
  recipe-maintainers:
    - kmuehlbauer
